I"<p>The port the Hue bridge API web server will run on. This can be any free port on your system. However, all new Alexa devices require listen_port: 80. See <code>setcap</code> note below if this is set below <code>1024</code> when Home Assistant is ran as a non-root user.</p>
:ET