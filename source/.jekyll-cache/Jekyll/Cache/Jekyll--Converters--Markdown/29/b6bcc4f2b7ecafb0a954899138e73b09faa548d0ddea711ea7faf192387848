I"÷<p>DEPRECATED, it is now done using feature detection in the browser. Version of the JavaScript to serve to clients. Options: <code>es5</code> - transpiled so old browsers understand it.  <code>latest</code> - not transpiled, so will work on recent browsers only. <code>auto</code> - select a version according to the browser user-agent. The value in the config can be overiden by putting <code>es5</code> or <code>latest</code> in the URL. For example <code>http://localhost:8123/states?es5</code></p>
:ET