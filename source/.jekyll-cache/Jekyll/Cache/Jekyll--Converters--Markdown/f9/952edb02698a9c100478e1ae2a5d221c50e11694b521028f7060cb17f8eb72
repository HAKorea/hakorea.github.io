I"|<p>The URL that Home Assistant is available on the internet. For example: <code>https://hass-example.duckdns.org:8123</code>. The iOS app finds local installations, if you have an outside URL use this so that you can auto-fill when discovered in the app. Note that this setting may only contain a protocol, hostname and port; using a path is <em>not</em> currently supported.</p>
:ET