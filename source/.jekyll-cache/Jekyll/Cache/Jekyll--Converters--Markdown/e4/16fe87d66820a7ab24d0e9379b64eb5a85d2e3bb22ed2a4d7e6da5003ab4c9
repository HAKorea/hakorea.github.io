I"�<p>The <code>openhardwaremonitor</code> platform uses your <a href="https://openhardwaremonitor.org/">Open Hardware Monitor</a> installation as a source for sensors that will display system information.</p>
<h2>Setup</h2>
<p>OpenHardwareMonitor must be running on the host, with “Remote web server” active. You also need to open inbound port (TCP 8085) on the host..</p>
<p>To open port (on Windows):</p>
<ol>
<li>Navigate to Control Panel, System and Security and Windows Firewall.</li>
<li>Select <strong>Advanced settings</strong> and highlight <strong>Inbound Rules</strong> in the left pane.</li>
<li>Right click Inbound Rules and select New Rule.</li>
<li>Add the port you need to open and click Next.</li>
<li>Add the protocol (TCP) and the port number (8085) into the next window and click Next.</li>
<li>Select Allow the connection in the next window and hit Next.</li>
<li>Select the network type as you see fit and click Next.</li>
<li>Name the rule and click Finish.</li>
</ol>
<p>To open port with <code>firewalld</code> (Linux):</p>
<pre><code class="language-bash">sudo firewall-cmd --permanent --add-port=8085/tcp
sudo firewall-cmd --reload
</code></pre>
<h2>Configuration</h2>
<p>To add Open Hardware Monitor to your installation, add the following to your <code>configuration.yaml</code> file:</p>
<pre><code class="language-yaml"># Example configuration.yaml entry
sensor:
  - platform: openhardwaremonitor
    host: IP_ADDRESS
</code></pre>
<div class="config-vars">
  <h3><a class="title-link" name="configuration-variables" href="#configuration-variables"></a> Configuration Variables</h3>
  <dl class=''><dt><a class='title-link' name='host' href='#host'></a> host</dt><dd><p class='desc'><span class='type'>(<span class='string'>string</span>)</span><span class='required'>(Required)</span><span class='description'><p>The IP address or hostname of the system where Open Hardware Monitor is running.</p>
</span></p></dd><dt><a class='title-link' name='port' href='#port'></a> port</dt><dd><p class='desc'><span class='type'>(<span class='integer'>integer</span>)</span><span class='required'>(Optional)</span><span class='description'><p>The port of your Open Hardware Monitor API. Defaults to 8085.</p>
</span></p></dd></dl>
</div>
:ET