I"Ç<p>Infer the disarmed arming state only via system status events. This works around a bug with some panels (<code>&lt;v5.8</code>) which emit <code>update.status = []</code> when they are armed.</p>
:ET