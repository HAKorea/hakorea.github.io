I"í<p>If given, this will act as a sensor that runs in the background and updates the state of the cover. If the command returns a <code>0</code> the indicates the cover is fully closed, whereas a 100 indicates the cover is fully open.</p>
:ET