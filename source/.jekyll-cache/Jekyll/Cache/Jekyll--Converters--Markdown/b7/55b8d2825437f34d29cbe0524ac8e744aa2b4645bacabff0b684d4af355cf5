I"Ð<p>This is the host where the nx584 server process is running. If unset, it is assumed to be <code>localhost</code>, which will work if the server process is running on the same system as Home Assistant.</p>
:ET